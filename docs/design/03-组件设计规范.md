# MultiForms 组件设计规范

| 文档版本 | 1.0 |
|----------|-----|
| 创建日期 | 2026-01-29 |
| 项目名称 | MultiForms |
| 文档类型 | 组件规范 |

---

## 1. 组件概览

### 1.1 组件分类
```
components/
├── navigation/      # 导航组件
│   ├── Navbar
│   ├── Sidebar
│   ├── TabBar
│   └── Breadcrumb
├── buttons/         # 按钮组件
│   ├── Button
│   ├── IconButton
│   └── ButtonGroup
├── forms/           # 表单组件
│   ├── Input
│   ├── Textarea
│   ├── Select
│   ├── Checkbox
│   ├── Radio
│   ├── Switch
│   ├── Slider
│   ├── DatePicker
│   ├── FileUpload
│   ├── Matrix
│   └── Sorting
├── cards/           # 卡片组件
│   ├── StatCard
│   ├── FormCard
│   └── FeatureCard
├── feedback/        # 反馈组件
│   ├── Toast
│   ├── ConfirmDialog
│   ├── Modal
│   ├── Alert
│   ├── Loading
│   └── ErrorBoundary
├── charts/          # 图表组件
│   ├── LineChart
│   ├── BarChart
│   ├── PieChart
│   └── ProgressBar
└── display/         # 展示组件
    ├── Avatar
    ├── Badge
    ├── Tag
    └── EmptyState
```

---

## 2. 导航组件

### 2.1 Navbar 顶部导航

**变体**:
- Default - 标准顶部导航
- Floating - 浮动玻璃态导航
- Transparent - 透明导航

**Floating Navbar**:
```html
<nav class="navbar-floating">
  <div class="navbar-container">
    <div class="navbar-brand">
      <svg class="logo">...</svg>
      <span>MultiForms</span>
    </div>
    <div class="navbar-menu">
      <a href="/features">功能</a>
      <a href="/templates">模板</a>
      <a href="/pricing">价格</a>
      <a href="/about">关于</a>
    </div>
    <div class="navbar-actions">
      <button class="btn-ghost">登录</button>
      <button class="btn-primary">免费注册</button>
    </div>
  </div>
</nav>
```

**样式规格**:
| 属性 | 值 |
|------|-----|
| 高度 | 64px |
| 定位 | fixed |
| 圆角 | 20px |
| 背景点 | rgba(15, 15, 35, 0.8) |
| 模糊 | 20px |

### 2.2 Sidebar 侧边栏

**状态**:
- Expanded (280px)
- Collapsed (80px)
- Hidden (0px)

```html
<aside class="sidebar sidebar-expanded">
  <div class="sidebar-header">
    <div class="logo">MultiForms</div>
  </div>
  <nav class="sidebar-nav">
    <a href="/dashboard" class="nav-item active">
      <svg>...</svg>
      <span>仪表盘</span>
    </a>
    <a href="/forms" class="nav-item">
      <svg>...</svg>
      <span>表单管理</span>
    </a>
    <!-- ... -->
  </nav>
  <div class="sidebar-footer">
    <div class="user-profile">...</div>
  </div>
</aside>
```

### 2.3 TabBar 底部导航 (移动端)

```html
<nav class="tabbar">
  <a href="/home" class="tab-item active">
    <svg>...</svg>
    <span>首页</span>
  </a>
  <a href="/forms" class="tab-item">
    <svg>...</svg>
    <span>表单</span>
  </a>
  <a href="/stats" class="tab-item">
    <svg>...</svg>
    <span>数据</span>
  </a>
  <a href="/profile" class="tab-item">
    <svg>...</svg>
    <span>我的</span>
  </a>
</nav>
```

### 2.4 Breadcrumb 面包屑导航

```html
<nav class="breadcrumb">
  <a href="/" class="breadcrumb-item">
    <svg class="breadcrumb-icon">🏠</svg>
    首页
  </a>
  <span class="breadcrumb-separator">/</span>
  <a href="/dashboard" class="breadcrumb-item">仪表盘</a>
  <span class="breadcrumb-separator">/</span>
  <a href="/forms" class="breadcrumb-item">表单管理</a>
  <span class="breadcrumb-separator">/</span>
  <span class="breadcrumb-item active">表单详情</span>
</nav>
```

**样式规格**:
```css
.breadcrumb {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 0;
}

.breadcrumb-item {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--text-secondary);
  text-decoration: none;
  font-size: 14px;
  transition: color 0.2s ease;
}

.breadcrumb-item:hover {
  color: var(--primary);
}

.breadcrumb-item.active {
  color: var(--text-primary);
  pointer-events: none;
}

.breadcrumb-separator {
  color: var(--text-muted);
  font-size: 12px;
}
```

---

## 3. 按钮组件

### 3.1 Button 基础按钮

**变体**:
```css
.btn-primary    /* 主按钮 - 渐变背景 */
.btn-secondary  /* 次要按钮 - 边框样式 */
.btn-ghost      /* 幽灵按钮 - 透明背景 */
.btn-danger     /* 危险按钮 - 红色 */
.btn-success    /* 成功按钮 - 绿色 */
```

**尺寸**:
```css
.btn-sm   /* 32px 高度 */
.btn-md   /* 40px 高度 - 默认 */
.btn-lg   /* 48px 高度 */
.btn-xl   /* 56px 高度 */
```

**完整示例**:
```html
<!-- Primary -->
<button class="btn btn-primary btn-md">
  开始使用
</button>

<!-- Secondary -->
<button class="btn btn-secondary btn-md">
  了解更多
</button>

<!-- Ghost -->
<button class="btn btn-ghost btn-md">
  取消
</button>

<!-- With Icon -->
<button class="btn btn-primary btn-md">
  <svg class="icon-left">...</svg>
  创建表单
  <svg class="icon-right">...</svg>
</button>
```

**CSS 变量**:
```css
--btn-radius-sm: 8px;
--btn-radius-md: 12px;
--btn-radius-lg: 16px;

--btn-padding-sm: 8px 16px;
--btn-padding-md: 12px 24px;
--btn-padding-lg: 16px 32px;

--btn-transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
```

### 3.2 IconButton 图标按钮

```html
<button class="btn-icon" aria-label="设置">
  <svg width="20" height="20">...</svg>
</button>

<button class="btn-icon btn-icon-lg" aria-label="添加">
  <svg width="24" height="24">...</svg>
</button>
```

**尺寸**:
| 类名 | 尺寸 |
|------|------|
| btn-icon-sm | 32×32px |
| btn-icon | 40×40px |
| btn-icon-lg | 48×48px |

---

## 4. 表单组件

### 4.1 Input 输入框

**状态**:
- Default - 默认状态
- Focus - 聚焦状态
- Error - 错误状态
- Disabled - 禁用状态
- Success - 成功状态

```html
<!-- Default -->
<div class="input-group">
  <label class="input-label">邮箱地址</label>
  <input
    type="email"
    class="input"
    placeholder="your@email.com"
  />
  <span class="input-helper">我们将向此邮箱发送验证链接</span>
</div>

<!-- With Icon -->
<div class="input-group">
  <div class="input-wrapper">
    <svg class="input-icon">...</svg>
    <input type="text" class="input input-with-icon" />
  </div>
</div>

<!-- Error State -->
<div class="input-group input-error">
  <label class="input-label">密码</label>
  <input type="password" class="input" />
  <span class="input-error">密码至少需要8个字符</span>
</div>
```

**样式规格**:
```css
.input {
  height: 48px;
  padding: 0 16px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: var(--input-bg);
  transition: var(--input-transition);
}

.input:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
}

.input-error {
  border-color: var(--error);
}

.input-error:focus {
  box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.2);
}
```

### 4.2 Select 下拉选择

```html
<div class="select-group">
  <label class="select-label">表单类型</label>
  <div class="select-wrapper">
    <select class="select">
      <option>投票</option>
      <option>评分</option>
      <option>调研问卷</option>
      <option>信息收集</option>
      <option>反馈表</option>
    </select>
    <svg class="select-icon">▼</svg>
  </div>
</div>
```

### 4.3 Checkbox 复选框

```html
<label class="checkbox">
  <input type="checkbox" class="checkbox-input" />
  <span class="checkbox-box">
    <svg class="checkbox-check">✓</svg>
  </span>
  <span class="checkbox-label">同意服务条款</span>
</label>
```

### 4.4 Radio 单选框

```html
<div class="radio-group">
  <label class="radio">
    <input type="radio" name="plan" class="radio-input" />
    <span class="radio-box"></span>
    <span class="radio-label">免费计划</span>
  </label>
  <label class="radio">
    <input type="radio" name="plan" class="radio-input" />
    <span class="radio-box"></span>
    <span class="radio-label">专业计划</span>
  </label>
</div>
```

### 4.5 Switch 开关

```html
<label class="switch">
  <input type="checkbox" class="switch-input" />
  <span class="switch-track">
    <span class="switch-thumb"></span>
  </span>
  <span class="switch-label">启用通知</span>
</label>
```

### 4.6 Slider 滑动条

```html
<div class="slider-group">
  <div class="slider-header">
    <label class="slider-label">满意度评分</label>
    <span class="slider-value">7.5</span>
  </div>
  <input
    type="range"
    min="0"
    max="10"
    step="0.5"
    value="7.5"
    class="slider"
  />
  <div class="slider-labels">
    <span>非常不满意</span>
    <span>非常满意</span>
  </div>
</div>
```

### 4.7 Textarea 多行文本输入

```html
<div class="textarea-group">
  <label class="textarea-label">详细反馈</label>
  <textarea
    class="textarea"
    placeholder="请输入您的反馈..."
    rows="4"
  ></textarea>
  <span class="textarea-counter">0/500</span>
</div>
```

**样式规格**:
```css
.textarea {
  min-height: 120px;
  padding: 12px 16px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: var(--input-bg);
  resize: vertical;
  font-family: 'DM Sans', sans-serif;
  line-height: 1.6;
}

.textarea:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
  outline: none;
}
```

### 4.8 DatePicker 日期选择器

```html
<div class="date-picker-group">
  <label class="date-picker-label">选择日期</label>
  <div class="date-picker-wrapper">
    <input type="date" class="date-picker" />
    <svg class="date-picker-icon">📅</svg>
  </div>
</div>
```

**样式规格**:
```css
.date-picker {
  width: 100%;
  height: 48px;
  padding: 0 16px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: var(--input-bg);
  color-scheme: dark;
}
```

### 4.9 FileUpload 文件上传组件

```html
<div class="file-upload-group">
  <label class="file-upload-label">上传文件</label>
  <div class="file-upload-zone">
    <svg class="file-upload-icon">📁</svg>
    <p class="file-upload-text">拖拽文件到此处或点击选择</p>
    <p class="file-upload-hint">支持 JPG, PNG, PDF，最大 10MB</p>
    <input type="file" class="file-upload-input" />
  </div>
  <div class="file-upload-list">
    <div class="file-upload-item">
      <span class="file-name">document.pdf</span>
      <button class="file-remove">×</button>
    </div>
  </div>
</div>
```

**样式规格**:
```css
.file-upload-zone {
  border: 2px dashed var(--border);
  border-radius: 12px;
  padding: 32px;
  text-align: center;
  cursor: pointer;
  transition: all 0.2s ease;
}

.file-upload-zone:hover,
.file-upload-zone.drag-over {
  border-color: var(--primary);
  background: rgba(99, 102, 241, 0.05);
}
```

### 4.10 Matrix 矩阵题组件

```html
<div class="matrix-question">
  <label class="matrix-label">请对以下项目进行评分</label>
  <div class="matrix-table">
    <div class="matrix-header">
      <div class="matrix-corner"></div>
      <div class="matrix-column">非常不同意</div>
      <div class="matrix-column">不同意</div>
      <div class="matrix-column">中立</div>
      <div class="matrix-column">同意</div>
      <div class="matrix-column">非常同意</div>
    </div>
    <div class="matrix-row">
      <div class="matrix-row-label">产品质量</div>
      <div class="matrix-cell"><input type="radio" name="m1" /></div>
      <div class="matrix-cell"><input type="radio" name="m1" /></div>
      <div class="matrix-cell"><input type="radio" name="m1" /></div>
      <div class="matrix-cell"><input type="radio" name="m1" /></div>
      <div class="matrix-cell"><input type="radio" name="m1" /></div>
    </div>
    <div class="matrix-row">
      <div class="matrix-row-label">服务质量</div>
      <div class="matrix-cell"><input type="radio" name="m2" /></div>
      <div class="matrix-cell"><input type="radio" name="m2" /></div>
      <div class="matrix-cell"><input type="radio" name="m2" /></div>
      <div class="matrix-cell"><input type="radio" name="m2" /></div>
      <div class="matrix-cell"><input type="radio" name="m2" /></div>
    </div>
  </div>
</div>
```

### 4.11 Sorting 排序题组件

```html
<div class="sorting-question">
  <label class="sorting-label">请将以下选项按优先级排序</label>
  <div class="sorting-list">
    <div class="sorting-item" draggable="true">
      <span class="sorting-handle">⋮⋮</span>
      <span class="sorting-text">选项 A</span>
      <span class="sorting-badge">1</span>
    </div>
    <div class="sorting-item" draggable="true">
      <span class="sorting-handle">⋮⋮</span>
      <span class="sorting-text">选项 B</span>
      <span class="sorting-badge">2</span>
    </div>
    <div class="sorting-item" draggable="true">
      <span class="sorting-handle">⋮⋮</span>
      <span class="sorting-text">选项 C</span>
      <span class="sorting-badge">3</span>
    </div>
  </div>
</div>
```

**样式规格**:
```css
.sorting-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: 8px;
  cursor: grab;
  transition: all 0.2s ease;
}

.sorting-item:active {
  cursor: grabbing;
}

.sorting-item.dragging {
  opacity: 0.5;
  transform: scale(1.02);
}
```

---

## 5. 卡片组件

### 5.1 StatCard 统计卡片

```html
<div class="stat-card stat-card-trend-up">
  <div class="stat-icon">
    <svg>...</svg>
  </div>
  <div class="stat-content">
    <div class="stat-label">总回复数</div>
    <div class="stat-value">8,521</div>
    <div class="stat-change">+12% 较上周</div>
  </div>
</div>
```

**变体**:
- stat-card-trend-up - 上升趋势 (绿色)
- stat-card-trend-down - 下降趋势 (红色)
- stat-card-neutral - 中性 (灰色)

### 5.2 FormCard 表单卡片

```html
<div class="form-card">
  <div class="form-card-header">
    <div class="form-type-badge">
      <svg>...</svg>
      <span>投票</span>
    </div>
    <button class="form-menu-btn">⋮</button>
  </div>
  <div class="form-card-body">
    <h3 class="form-title">年度最佳员工评选</h3>
    <p class="form-description">请选出您心目中的...</p>
  </div>
  <div class="form-card-stats">
    <span class="stat">128 投票</span>
    <span class="stat">·</span>
    <span class="stat">2天前</span>
  </div>
  <div class="form-card-actions">
    <button class="btn-ghost-sm">编辑</button>
    <button class="btn-ghost-sm">分享</button>
    <button class="btn-primary-sm">查看</button>
  </div>
</div>
```

### 5.3 FeatureCard 功能卡片

```html
<div class="feature-card">
  <div class="feature-icon-wrapper">
    <svg class="feature-icon">...</svg>
  </div>
  <h3 class="feature-title">拖拽式创建</h3>
  <p class="feature-description">
    无需任何代码，通过直观的拖拽界面，
    轻松创建专业表单
  </p>
  <a href="#" class="feature-link">
    了解更多
    <svg class="feature-arrow">→</svg>
  </a>
</div>
```

---

## 6. 反馈组件

### 6.1 Toast 通知

**位置变体**:
- toast-top-right
- toast-top-center
- toast-bottom-right
- toast-bottom-center

**类型变体**:
```html
<!-- Success -->
<div class="toast toast-success">
  <svg class="toast-icon">✓</svg>
  <span class="toast-message">保存成功</span>
  <button class="toast-close">×</button>
</div>

<!-- Error -->
<div class="toast toast-error">
  <svg class="toast-icon">✕</svg>
  <span class="toast-message">操作失败，请重试</span>
  <button class="toast-close">×</button>
</div>

<!-- Info -->
<div class="toast toast-info">
  <svg class="toast-icon">ⓘ</svg>
  <span class="toast-message">新功能上线！</span>
  <button class="toast-close">×</button>
</div>
```

### 6.2 ConfirmDialog 确认对话框

用于替换原生的 `confirm()` API，提供更美观的确认对话框。

**使用方式**:
```tsx
import { useConfirm } from '@/components/shared/confirm-dialog'

function MyComponent() {
  const { confirm } = useConfirm()

  const handleDelete = async () => {
    const confirmed = await confirm({
      title: '确认删除',
      message: '此操作无法撤销，确认要删除这个表单吗？',
      confirmText: '确认删除',
      cancelText: '取消',
      variant: 'danger',
    })

    if (confirmed) {
      // 执行删除操作
    }
  }

  return <button onClick={handleDelete}>删除</button>
}
```

**变体**:
- `default` - 默认样式（蓝色主按钮）
- `danger` - 危险操作（红色主按钮）
- `warning` - 警告操作（橙色主按钮）

**样式规格**:
| 属性 | 值 |
|------|-----|
| 宽度 | 400px |
| 圆角 | 16px |
| 背景模糊 | 20px |
| 动画 | 200ms ease-out |

### 6.3 Modal 模态框

**尺寸**:
- modal-sm - 400px
- modal-md - 560px (默认)
- modal-lg - 720px
- modal-xl - 960px

```html
<div class="modal-overlay">
  <div class="modal modal-md">
    <div class="modal-header">
      <h2 class="modal-title">确认删除</h2>
      <button class="modal-close">×</button>
    </div>
    <div class="modal-body">
      <p>此操作无法撤销，确认要删除这个表单吗？</p>
    </div>
    <div class="modal-footer">
      <button class="btn btn-secondary">取消</button>
      <button class="btn btn-danger">确认删除</button>
    </div>
  </div>
</div>
```

### 6.4 Alert 警告提示

```html
<div class="alert alert-info">
  <svg class="alert-icon">ⓘ</svg>
  <div class="alert-content">
    <div class="alert-title">提示</div>
    <div class="alert-message">您的账户需要验证邮箱</div>
  </div>
  <button class="alert-close">×</button>
</div>
```

### 6.5 Loading 加载状态

**变体**:
```html
<!-- Spinner -->
<div class="spinner"></div>

<!-- Dots -->
<div class="loading-dots">
  <span></span>
  <span></span>
  <span></span>
</div>

<!-- Skeleton Card -->
<div class="skeleton-card">
  <div class="skeleton-avatar"></div>
  <div class="skeleton-lines">
    <div class="skeleton-line"></div>
    <div class="skeleton-line skeleton-line-short"></div>
  </div>
</div>
```

### 6.6 ErrorBoundary 错误边界

用于捕获 React 组件树中的 JavaScript 错误，防止整个应用崩溃，并显示友好的错误提示。

**使用方式**:
```tsx
import { ErrorBoundary } from '@/components/error-boundary'

function App() {
  return (
    <ErrorBoundary
      fallback={
        <div className="error-fallback">
          <h2>出错了</h2>
          <p>抱歉，应用遇到了一些问题</p>
          <button onClick={() => window.location.reload()}>
            重新加载
          </button>
        </div>
      }
    >
      <YourAppComponents />
    </ErrorBoundary>
  )
}
```

**Props**:
| Prop | 类型 | 默认值 | 描述 |
|------|------|--------|------|
| fallback | ReactNode | 默认错误UI | 错误时显示的内容 |
| onError | (error: Error) => void | undefined | 错误回调函数 |

**样式规格**:
```css
.error-fallback {
  min-height: 400px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px;
  text-align: center;
}

.error-fallback h2 {
  font-size: 24px;
  color: var(--text-primary);
  margin-bottom: 16px;
}

.error-fallback p {
  color: var(--text-secondary);
  margin-bottom: 24px;
}
```

---

## 7. 图表组件

### 7.1 LineChart 折线图

```html
<div class="chart-container">
  <div class="chart-header">
    <h3 class="chart-title">回复趋势</h3>
    <div class="chart-legend">
      <span class="legend-item">
        <span class="legend-dot"></span>
        本周
      </span>
    </div>
  </div>
  <canvas id="trendChart"></canvas>
</div>
```

### 7.2 BarChart 柱状图

```html
<div class="chart-container">
  <div class="chart-header">
    <h3 class="chart-title">选项分布</h3>
  </div>
  <div class="bar-chart">
    <div class="bar-item">
      <div class="bar-label">选项 A</div>
      <div class="bar-track">
        <div class="bar-fill" style="width: 65%"></div>
      </div>
      <div class="bar-value">65%</div>
    </div>
    <!-- ... -->
  </div>
</div>
```

### 7.3 ProgressBar 进度条

```html
<div class="progress-group">
  <div class="progress-header">
    <span class="progress-label">完成度</span>
    <span class="progress-value">75%</span>
  </div>
  <div class="progress-track">
    <div class="progress-fill" style="width: 75%"></div>
  </div>
</div>
```

---

## 8. 展示组件

### 8.1 Avatar 头像

**尺寸**:
- avatar-xs - 24px
- avatar-sm - 32px
- avatar-md - 40px
- avatar-lg - 48px
- avatar-xl - 64px

```html
<!-- With Image -->
<div class="avatar avatar-md">
  <img src="..." alt="用户名" />
</div>

<!-- With Initials -->
<div class="avatar avatar-md avatar-initials">
  <span>张三</span>
</div>

<!-- With Status -->
<div class="avatar avatar-lg">
  <img src="..." alt="用户名" />
  <span class="avatar-status avatar-status-online"></span>
</div>
```

### 8.2 Badge 徽章

```html
<!-- Default -->
<span class="badge">新功能</span>

<!-- Primary -->
<span class="badge badge-primary">Pro</span>

<!-- Success -->
<span class="badge badge-success">已完成</span>

<!-- Warning -->
<span class="badge badge-warning">待审核</span>

<!-- Error -->
<span class="badge badge-error">已过期</span>
```

### 8.3 Tag 标签

```html
<div class="tag-group">
  <span class="tag">投票</span>
  <span class="tag">问卷调查</span>
  <span class="tag tag-removable">
    评分
    <button class="tag-remove">×</button>
  </span>
</div>
```

### 8.4 EmptyState 空状态

```html
<div class="empty-state">
  <div class="empty-icon">
    <svg>...</svg>
  </div>
  <h3 class="empty-title">还没有表单</h3>
  <p class="empty-description">
    创建您的第一个表单，开始收集数据
  </p>
  <button class="btn btn-primary">创建表单</button>
</div>
```

### 8.5 ThemeSelector 主题选择器

```html
<!-- Theme Color Selector -->
<div class="theme-selector">
  <button class="theme-option active" data-theme="nebula">
    <span class="theme-dot" style="background: linear-gradient(135deg, #6366F1, #8B5CF6)"></span>
    <span class="theme-name">星云紫</span>
  </button>
  <button class="theme-option" data-theme="ocean">
    <span class="theme-dot" style="background: linear-gradient(135deg, #0EA5E9, #06B6D4)"></span>
    <span class="theme-name">海洋蓝</span>
  </button>
  <button class="theme-option" data-theme="sunset">
    <span class="theme-dot" style="background: linear-gradient(135deg, #F97316, #EC4899)"></span>
    <span class="theme-name">日落橙</span>
  </button>
  <!-- More themes... -->
</div>
```

**样式规格:**
```css
.theme-option {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  border-radius: 20px;
  border: 2px solid var(--border);
  background: var(--bg-secondary);
  transition: all 0.3s var(--ease-out);
}

.theme-option.active {
  border-color: var(--primary-start);
  background: linear-gradient(135deg, var(--primary-start), var(--primary-end));
}

.theme-dot {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,0.3);
}
```

### 8.6 ThemePreviewCard 主题预览卡片

```html
<!-- Theme Preview Card -->
<div class="theme-preview-card">
  <div class="theme-preview-canvas">
    <!-- Mini form preview with applied theme -->
    <div class="mini-form">
      <div class="mini-title">表单标题</div>
      <div class="mini-button">提交按钮</div>
    </div>
  </div>
  <div class="theme-info">
    <h4 class="theme-title">星云紫 Nebula Purple</h4>
    <p class="theme-desc">神秘优雅的紫色调，营造专业现代的视觉体验</p>
  </div>
  <button class="btn btn-primary btn-sm">应用主题</button>
</div>
```

**样式规格:**
```css
.theme-preview-card {
  padding: 20px;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  text-align: center;
  transition: all 0.3s var(--ease-out);
}

.theme-preview-card:hover {
  transform: translateY(-4px);
  border-color: var(--primary-start);
}

.theme-preview-canvas {
  min-height: 120px;
  background: var(--bg-primary);
  border-radius: var(--radius-md);
  margin-bottom: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

---

## 9. 动画规范

### 9.1 页面进入动画

```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-in {
  animation: fadeInUp 0.6s ease-out forwards;
}

/* 延迟变体 */
.animate-delay-100 { animation-delay: 0.1s; }
.animate-delay-200 { animation-delay: 0.2s; }
.animate-delay-300 { animation-delay: 0.3s; }
```

### 9.2 Hover 动画

```css
.hover-lift {
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.hover-lift:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
}
```

### 9.3 加载动画

```css
@keyframes spin {
  to { transform: rotate(360deg); }
}

.spinner {
  animation: spin 1s linear infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.pulse {
  animation: pulse 2s ease-in-out infinite;
}
```

---

## 10. 响应式规范

### 10.1 断点工具类

```css
/* Mobile First */
@media (min-width: 640px) { /* sm */ }
@media (min-width: 768px) { /* md */ }
@media (min-width: 1024px) { /* lg */ }
@media (min-width: 1280px) { /* xl */ }
```

### 10.2 网格系统

```css
.grid {
  display: grid;
  gap: 16px;
}

.grid-cols-1 { grid-template-columns: repeat(1, 1fr); }
.grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
.grid-cols-3 { grid-template-columns: repeat(3, 1fr); }
.grid-cols-4 { grid-template-columns: repeat(4, 1fr); }

/* 响应式网格 */
.grid-responsive {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}
```

---

*组件设计规范与 UI/UX 设计系统配合使用，确保所有组件的一致性。*
