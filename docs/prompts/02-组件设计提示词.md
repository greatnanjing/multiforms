# 组件设计提示词

本文档包含 React 组件设计的提示词模板，基于 MultiForms 设计系统。

## 基础模板

### 提示词：创建 React 组件

```
请为 MultiForms 项目创建一个 [组件名称] React 组件。

# 项目技术栈

- **框架**: React 18+ with TypeScript
- **样式**: Tailwind CSS + shadcn/ui
- **状态管理**: Zustand / React Context
- **表单**: React Hook Form + Zod

# 设计系统

## 颜色
```css
/* Tailwind 配置 */
colors: {
  primary: {
    DEFAULT: '#6366F1',
    start: '#6366F1',
    end: '#8B5CF6',
    glow: '#A78BFA'
  },
  accent: {
    pink: '#EC4899',
    cyan: '#06B6D4'
  },
  background: {
    primary: '#0F0F23',
    secondary: '#1A1A2E',
    tertiary: '#2D2D44'
  },
  text: {
    primary: '#F8FAFC',
    secondary: '#94A3B8',
    muted: '#64748B'
  }
}
```

## 字体
- Headings: Space Grotesk
- Body: DM Sans
- Code: JetBrains Mono

## 圆角
- sm: 8px
- md: 12px
- lg: 20px
- xl: 24px

## 阴影
- shadow-glow: 0 4px 20px rgba(99, 102, 241, 0.3)
- shadow-lg: 0 20px 40px rgba(0, 0, 0, 0.3)

# 组件要求

## 组件名称
[组件名称]

## 组件功能
[描述组件的主要功能和用途]

## Props 接口
```typescript
interface [组件名]Props {
  // 列出所有必要的 props
  [propName]: [propType];
}
```

## 样式要求
- 使用 Tailwind CSS
- 遵循玻璃态设计风格（如适用）
- 支持深色主题
- 响应式设计

## 交互要求
- Hover 状态
- Focus 状态（可聚焦元素）
- Loading 状态（如适用）
- Error 状态（如适用）
- Disabled 状态（如适用）

## 可访问性
- 正确的 ARIA 属性
- 键盘导航支持
- 屏幕阅读器友好

# 输出要求

1. 创建文件: `src/components/[分类]/[组件名].tsx`
2. 导入必要的依赖
3. 定义 TypeScript 接口
4. 实现组件逻辑
5. 添加 JSDoc 注释
6. 导出组件

【自动验证】
完成后请自动执行以下验证：
1. 检查组件文件是否已创建
2. 验证 TypeScript 类型是否正确定义
3. 运行 npx tsc --noEmit 检查类型错误
4. 报告验证结果，如有问题请修复

请开始创建这个组件。
```

## 组件分类模板

### 1. 基础 UI 组件

#### Button 按钮

```
# Button 组件要求

## Props
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'outline' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  isLoading?: boolean;
  leftIcon?: React.ReactNode;
  rightIcon?: React.ReactNode;
  fullWidth?: boolean;
}
```

## Variants 样式
- primary: 渐变背景，白色文字
- secondary: 透明背景，边框
- ghost: 仅文字，hover 背景变化
- outline: 透明背景，边框
- danger: 红色背景

## 尺寸
- sm: padding 8px 16px, font-size 14px
- md: padding 12px 24px, font-size 16px
- lg: padding 16px 32px, font-size 18px
```

#### Input 输入框

```
# Input 组件要求

## Props
```typescript
interface InputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  label?: string;
  error?: string;
  helperText?: string;
  leftIcon?: React.ReactNode;
  rightIcon?: React.ReactNode;
  size?: 'sm' | 'md' | 'lg';
}
```

## 样式
- 透明背景 rgba(255, 255, 255, 0.03)
- 1px 边框 rgba(255, 255, 255, 0.1)
- Focus: 主题色边框 + 发光效果
- Error: 红色边框

## 状态
- 默认
- Focus
- Error
- Disabled
```

#### Card 卡片

```
# Card 组件要求

## Props
```typescript
interface CardProps {
  children: React.ReactNode;
  className?: string;
  hover?: boolean;
  glass?: boolean;
}
```

## 样式
- 默认: 纯色背景
- glass: 玻璃态效果 (blur(20px) saturate(180%))
- hover: 悬停抬升 + 边框高亮

## 结构
- Card.Header
- Card.Body
- Card.Footer
```

### 2. 表单组件

#### FormInput 表单输入

```
# FormInput 组件要求

## Props
```typescript
interface FormInputProps {
  name: string;
  label?: string;
  control: Control<any>;
  rules?: ValidationRule<any>[];
  placeholder?: string;
  helperText?: string;
  leftIcon?: React.ReactNode;
}
```

## 集成
- React Hook Form
- Zod 验证
- 自动错误显示
```

#### FormSelect 选择器

```
# FormSelect 组件要求

## Props
```typescript
interface FormSelectProps {
  name: string;
  label?: string;
  control: Control<any>;
  options: { label: string; value: string }[];
  placeholder?: string;
}
```

## 样式
- 自定义下拉样式
- 玻璃态下拉菜单
- 选项悬停效果
```

#### FormToggle 切换

```
# FormToggle 组件要求

## Props
```typescript
interface FormToggleProps {
  name: string;
  label?: string;
  control: Control<any>;
}
```

## 样式
- 自定义开关样式
- 滑块动画
- 主题色激活状态
```

### 3. 数据展示组件

#### StatCard 统计卡片

```
# StatCard 组件要求

## Props
```typescript
interface StatCardProps {
  title: string;
  value: string | number;
  change?: number;
  icon?: React.ReactNode;
  loading?: boolean;
}
```

## 样式
- Bento Grid 单元格
- 玻璃态背景
- 图标 + 数值布局
- 趋势指示器（上升/下降）
```

#### DataTable 数据表格

```
# DataTable 组件要求

## Props
```typescript
interface DataTableProps<T> {
  data: T[];
  columns: Column<T>[];
  pagination?: boolean;
  loading?: boolean;
}

interface Column<T> {
  key: keyof T;
  title: string;
  render?: (value: any, record: T) => React.ReactNode;
}
```

## 功能
- 斑马纹行
- 固定表头
- 排序
- 分页
- 空状态
```

#### Chart 图表

```
# Chart 组件要求

## Props
```typescript
interface ChartProps {
  type: 'line' | 'bar' | 'pie';
  data: ChartData;
  options?: ChartOptions;
}
```

## 实现
- 使用 Recharts / Chart.js
- 主题色适配
- 响应式
- 工具提示
```

### 4. 导航组件

#### Navbar 导航栏

```
# Navbar 组件要求

## Props
```typescript
interface NavbarProps {
  logo?: React.ReactNode;
  leftContent?: React.ReactNode;
  rightContent?: React.ReactNode;
  glass?: boolean;
}
```

## 样式
- 固定顶部
- 玻璃态背景
- 透明度滚动变化
```

#### Sidebar 侧边栏

```
# Sidebar 组件要求

## Props
```typescript
interface SidebarProps {
  items: MenuItem[];
  collapsed?: boolean;
  onCollapseChange?: (collapsed: boolean) => void;
}
```

## 样式
- 固定宽度 240px
- 折叠后 64px
- 收起动画
```

#### TabBar 标签栏（移动端）

```
# TabBar 组件要求

## Props
```typescript
interface TabBarProps {
  items: MenuItem[];
  activeKey: string;
  onChange?: (key: string) => void;
}
```

## 样式
- 固定底部
- 4-5 个标签
- 图标 + 文字
- 激活状态高亮
```

### 5. 反馈组件

#### Toast 提示

```
# Toast 组件要求

## Props
```typescript
interface ToastProps {
  type?: 'success' | 'error' | 'warning' | 'info';
  title?: string;
  message: string;
  duration?: number;
}
```

## 样式
- 玻璃态背景
- 右上角/顶部居中
- 进度条
- 滑入/滑出动画
```

#### Modal 模态框

```
# Modal 组件要求

## Props
```typescript
interface ModalProps {
  open: boolean;
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
  size?: 'sm' | 'md' | 'lg' | 'xl';
}
```

## 样式
- 背景遮罩模糊
- 玻璃态内容区
- 缩放进入动画
- 关闭按钮
```

#### Alert 警告

```
# Alert 组件要求

## Props
```typescript
interface AlertProps {
  type?: 'success' | 'error' | 'warning' | 'info';
  title?: string;
  message: string;
  closable?: boolean;
}
```

## 样式
- 浅色背景
- 左侧彩色条
- 图标 + 消息
- 关闭按钮
```

### 6. 表单构建器组件

#### FormBuilder 表单编辑器

```
# FormBuilder 组件要求

## Props
```typescript
interface FormBuilderProps {
  formId: string;
  initialQuestions?: Question[];
  onSave?: (questions: Question[]) => void;
}
```

## 功能
- 拖拽排序
- 题目增删改
- 实时预览
- 自动保存
```

#### QuestionCard 题目卡片

```
# QuestionCard 组件要求

## Props
```typescript
interface QuestionCardProps {
  question: Question;
  index: number;
  onEdit: () => void;
  onDelete: () => void;
  onDuplicate: () => void;
  draggable?: boolean;
}
```

## 功能
- 悬停显示操作按钮
- 拖拽手柄
- 题目类型标签
```

#### ToolPanel 工具面板

```
# ToolPanel 组件要求

## Props
```typescript
interface ToolPanelProps {
  questionTypes: QuestionType[];
  onAddQuestion: (type: QuestionType) => void;
  position?: 'left' | 'right' | 'bottom';
}
```

## 功能
- 题型分类
- 拖拽添加
- 移动端抽屉
```

## 组件模板代码

### 基础组件模板

```typescript
import React from 'react';
import { cn } from '@/lib/utils';

interface ComponentNameProps {
  className?: string;
  children?: React.ReactNode;
}

export const ComponentName: React.FC<ComponentNameProps> = ({
  className,
  children,
  ...props
}) => {
  return (
    <div className={cn('base-classes', className)} {...props}>
      {children}
    </div>
  );
};

ComponentName.displayName = 'ComponentName';
```

### 带变体的组件模板

```typescript
import React from 'react';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const buttonVariants = cva(
  'base-classes transition-all duration-200',
  {
    variants: {
      variant: {
        primary: 'bg-gradient-to-r from-primary-start to-primary-end text-white',
        secondary: 'bg-transparent border border-white/10',
      },
      size: {
        sm: 'px-4 py-2 text-sm',
        md: 'px-6 py-3 text-base',
        lg: 'px-8 py-4 text-lg',
      },
    },
    defaultVariants: {
      variant: 'primary',
      size: 'md',
    },
  }
);

interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  isLoading?: boolean;
}

export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, isLoading, children, ...props }, ref) => {
    return (
      <button
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        disabled={isLoading}
        {...props}
      >
        {isLoading ? 'Loading...' : children}
      </button>
    );
  }
);

Button.displayName = 'Button';
```

## 设计 Token

### Spacing（间距）

```css
--space-xs: 4px;
--space-sm: 8px;
--space-md: 16px;
--space-lg: 24px;
--space-xl: 32px;
--space-2xl: 48px;
--space-3xl: 64px;
```

### Z-Index（层级）

```css
--z-dropdown: 100;
--z-sticky: 200;
--z-fixed: 300;
--z-modal-backdrop: 400;
--z-modal: 500;
--z-popover: 600;
--z-tooltip: 700;
```

## 快速复用

### Tailwind 配置

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#6366F1',
          start: '#6366F1',
          end: '#8B5CF6',
          glow: '#A78BFA',
        },
        accent: {
          pink: '#EC4899',
          cyan: '#06B6D4',
        },
        background: {
          primary: '#0F0F23',
          secondary: '#1A1A2E',
          tertiary: '#2D2D44',
        },
      },
      fontFamily: {
        sans: ['DM Sans', 'sans-serif'],
        display: ['Space Grotesk', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      borderRadius: {
        'lg': '20px',
        'xl': '24px',
      },
    },
  },
};
```
